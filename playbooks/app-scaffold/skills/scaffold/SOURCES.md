# Sources

- Claude Code best practices, "Give Claude a way to verify its work": a check that returns pass or fail closes the loop (the stack's verification commands must pass before the pull request): https://code.claude.com/docs/en/best-practices
- GitHub Docs, building and testing Node.js: `actions/checkout`, `actions/setup-node` with a Node version and dependency cache, a clean install, then build and test: https://docs.github.com/en/actions/tutorials/build-and-test-code/nodejs
- Docker, "Multi-stage builds": copy only what the final image needs: https://docs.docker.com/build/building/multi-stage/
- The stack skills of `playbooks/app-stack` (layout, conventions, verification) and their own sources.

Researched 2026-09-25.
