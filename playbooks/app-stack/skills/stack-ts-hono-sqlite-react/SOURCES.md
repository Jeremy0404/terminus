# Sources

- Hono: "a small, simple, and ultrafast web framework built on Web Standards" that runs on Node.js among other runtimes: https://hono.dev/docs/
- Drizzle with SQLite: native support for the `libsql`, `node:sqlite` and `better-sqlite3` drivers; migrations generated with `drizzle-kit generate`: https://orm.drizzle.team/docs/get-started-sqlite
- React, "Build a React app from scratch": Vite with the `react-ts` template: https://react.dev/learn/build-a-react-app-from-scratch
- pnpm workspaces: https://pnpm.io/workspaces
- Terminus itself, which the user built on exactly this shape.

Researched 2026-09-25.
